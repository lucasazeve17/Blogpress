<%- include('../../partials/head')%>

<body>
    <%- include('../../partials/navbar')%>

    <div class="container">
        <hr>
        <div class="card">
            <div class="card-header">
                <h2>Editar de categorias</h2>
            </div>
            <div class="card-body">
                <form action="/categories/update?_method=put" method="POST">
                    <input class="form-control" type="text" name="title" placeholder="Defina o título da categoria" value="<%= category.title%>">
                    <button class="btn btn-success mrg-top" type="submit">Atualizar</button>
                    <input type="hidden" name="id" value="<%= category.id%>">
                </form>
            </div>
        </div>
    </div>
</body>

<%- include('../../partials/footer')%>